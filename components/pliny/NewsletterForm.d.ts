import * as react_jsx_runtime from 'react/jsx-runtime'

interface NewsletterFormProps {
  title?: string
  apiUrl?: string
}
declare const NewsletterForm: ({
  title,
  apiUrl,
}: NewsletterFormProps) => react_jsx_runtime.JSX.Element

export { NewsletterFormProps, NewsletterForm as default }
